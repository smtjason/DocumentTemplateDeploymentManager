# Power Platform ToolBox implementation

This directory is reserved for the Power Platform ToolBox port.

The port will use the Power Platform ToolBox TypeScript APIs and multi-connection capability while preserving the same deployment behavior, matching rules, DOCX rebinding rules, validation, and safety constraints as the XrmToolBox implementation.
